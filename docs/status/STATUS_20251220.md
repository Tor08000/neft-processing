# Status & Stage Freeze — 2025-12-20

## Current state
- Docker CLI is unavailable in the current environment (`docker compose up -d postgres redis` → `command not found`), so runtime checks and DB-backed Alembic commands could not be executed here. Static analysis of the compose file, gateways, and migrations was completed instead.
- docker-compose.yml defines the full stack (database, caches, storage, APIs, UI bundles, workers, observability). Gateway exposes the SPA frontends and proxies `/api/core/*`, `/api/auth/*`, `/api/ai/*` without path rewrites.
- Alembic has **two heads** (`20261201_0017_accounts_and_ledger` and `20271020_0033_billing_periods`) plus a merge revision (`20270215_0021_merge_heads`) that unifies four earlier branches. A single-head consolidation is required before new migrations land.
- Billing and ledger features exist in code (billing periods, commission rules, ledger entries) but were not exercised end-to-end because no database was available in this environment.

## Service inventory (docker-compose.yml)
| Service | Image / build | Exposed ports | Healthcheck | Depends on |
| --- | --- | --- | --- | --- |
| postgres | `postgres:16` | 5432 | `pg_isready -U neft -d neft` | — |
| redis | `redis:7.4-alpine` | 6379 | `redis-cli ping` | — |
| minio | `quay.io/minio/minio:RELEASE.2024-09-22T00-33-43Z` | 9000, 9001 | `curl -f http://localhost:9000/minio/health/live` | — |
| admin-web | build `frontends/admin-ui/Dockerfile` | 4173→80 | `curl -f http://localhost/health` | — |
| client-web | build `frontends/client-portal/Dockerfile` | 4174→80 | `curl -f http://localhost/health` | — |
| auth-host | build `platform/auth-host/Dockerfile` | 8002→8000 | Python HTTP 200 check on `/api/auth/health` | postgres (healthy), redis (healthy) |
| core-api | build `platform/processing-core/Dockerfile` | 8001→8000 | Python HTTP 200 check on `/api/core/health` | postgres (healthy), redis (healthy) |
| ai-service | build `platform/ai-services/risk-scorer/Dockerfile` | 8003→8000 | Python HTTP 200 check on `/api/v1/health` | redis (healthy) |
| crm-service | `alpine:3.20` | — | echo ok | — |
| logistics-service | `alpine:3.20` | — | echo ok | — |
| document-service | `alpine:3.20` | — | echo ok | — |
| workers | build `platform/billing-clearing/Dockerfile` | — | — | core-api (healthy), redis (healthy) |
| beat | build `platform/billing-clearing/Dockerfile` | — | — | core-api (healthy), redis (healthy) |
| flower | build `services/flower/Dockerfile` | 5555 | `curl -fsS .../api/workers?refresh=1` | redis (healthy) |
| gateway | build `gateway/Dockerfile` | 80 | `curl -f http://127.0.0.1/health` | core-api, auth-host, ai-service (healthy) |
| otel-collector | `otel/opentelemetry-collector-contrib:0.104.0` | 4317 | — | — |
| jaeger | `jaegertracing/all-in-one:1.55` | 16686, 14250 | `wget http://localhost:16686` | otel-collector (started) |
| prometheus | `prom/prometheus:v2.52.0` | 9090 | `wget http://localhost:9090/-/healthy` | gateway |
| grafana | `grafana/grafana:10.4.1` | 3000 | `wget http://localhost:3000/health` | prometheus (healthy) |

## API entry points
- **Core API** (default base `/api/core`, direct port 8001): health at `/health` and `/api/core/health`; DB and Celery checks at `/health/db` and `/health/celery`; Celery enqueue probe `/api/v1/health/enqueue`; Prometheus metrics at `/metrics` (alias `/metric`).
- **Auth-host** (default base `/api/auth`, direct port 8002): health at `/api/auth/health` and `/api/v1/auth/health`; legacy `/health` alias; OpenAPI at `/api/auth/openapi.json`.
- **AI service** (default base `/api/ai`, direct port 8003): health endpoints `/api/v1/health`, `/api/v1/live`, `/api/v1/ready` plus aliases at `/api/ai/health` and `/health`; metrics at `/metrics`; scoring routes under `/api/v1/score` (proxied via gateway `/api/ai/api/v1/...`).
- **Gateway (nginx)**: exposes `/health`, `/metrics` (static `gateway_up 1`), proxies `/api/core/`→core-api, `/api/auth/`→auth-host, `/api/ai/`→ai-service, legacy `/api/v1/`→core-api, and serves SPAs under `/admin/` and `/client/`.

## Migration status (processing-core)
- **Commands pending runtime verification**: `alembic -c app/alembic.ini heads/current/history` could not be run here because Docker is unavailable; use the Windows CMD checklist below on an environment with Docker/psql.
- **Heads detected from static analysis**: `20261201_0017_accounts_and_ledger` and `20271020_0033_billing_periods`.
- **Merge revision**: `20270215_0021_merge_heads` merges four branches (`20260110_0009_create_clearing_table`, `20260701_0009_client_portal`, `20270115_0020`, `20270201_0020`) without schema ops.
- **History (latest 50)**:
  - 20271020_0033_billing_periods → 0032_operational_scenarios_v1 → 0031_tariff_commission_rules / 0031_partner_settlements → 0030_billing_state_machine → 0029_cards_created_at → 0028_limit_config_scope_enum_fix → 0027_add_posting_result_to_operations → 0026_add_accounts_to_operations → 0025_ledger_entries_operation_id_nullable → 0024_bootstrap_schema → 0023_billing_summary_status_enum_fix → 0022_extend_alembic_version_len → 0021_merge_heads (merging four branches).
  - 20261201_0017_accounts_and_ledger → 0016_risk_rule_audit → 0015_risk_rules → 0014_billing_summary_alignment → 0013_operations_limits_alignment (alias) → 0013 → 0012_client_ids_uuid → 0011_operations_indexes → 0010_clearing → 0009_billing_summary_extend → 0008_billing_summary → 0007_add_capture_refund_fields_to_operations → 0006_auto_fix → 0005_add_created_at_to_cards → 0004a_bootstrap_clients_cards_partners → 0004_operations_product_fields → 0003_merchants_terminals_cards → 0003a_extend_alembic_version_len → 0003_limits_rules_v2 → 0002_operations_journal → 0001_core → 2025_11_01_init.
- **Open items**: consolidate to a single head and confirm `alembic_version` table contents once a database is available.

## Schema baseline summary
- **Core processing**: clients, cards, merchants, terminals, operations, posting_batches, external_request_logs, partners.
- **Ledger & accounts**: accounts, account_balances, ledger_entries, operations (accounts columns), posting_batches.
- **Limits & risk**: limits_rules, limit_configs, limit_rules (legacy), client/card groups & memberships, risk_rules, risk_rule_versions, risk_rule_audits.
- **Billing**: billing_summary, billing_periods, invoices, invoice_lines.
- **Settlements & payouts**: clearing, clearing_batch, clearing_batch_operation, settlements, payout_orders, payout_events.
- **Disputes / refunds / reversals**: dispute_events, disputes, refund_requests, reversals.
- **Tariffs / pricing / commissions**: tariff_plans, tariff_prices, client_tariffs, commission_rules, limit_configs.
- **Client portal**: client_cards, client_operations, client_limits.
- **Enums by domain**:
  - Processing/operations: OperationType, OperationStatus, ProductType, PostingBatchStatus, PostingBatchType, RiskResult.
  - Ledger/accounts: LedgerDirection, AccountType, AccountOwnerType, AccountStatus.
  - Billing: BillingSummaryStatus, BillingPeriodType, BillingPeriodStatus, InvoiceStatus.
  - Settlements/payouts: Clearing status enums, SettlementStatus, PayoutOrderStatus, SettlementPolicy (shared with reversals/refunds).
  - Disputes/refunds: DisputeStatus, DisputeEventType, RefundRequestStatus, ReversalStatus.
  - Limits & contracts: LimitEntityType, LimitScope, FuelProductType, LimitConfigScope, LimitType, LimitWindow.
  - Financial adjustments: FinancialAdjustmentKind, FinancialAdjustmentStatus, RelatedEntityType.
  - Risk rules: RiskRuleScope, RiskRuleAction, RiskRuleAuditAction.

## What works now (verified commands + expected outputs)
- `docker compose up -d postgres redis` **failed here** because Docker is missing; rerun in an environment with Docker to start dependencies.
- Expected health checks once stack is up (Gateway base `http://localhost`):
  - `curl http://localhost/health` → `OK`.
  - `curl http://localhost/api/core/health` → `{"status":"ok"}`.
  - `curl http://localhost/api/auth/health` → `{"status":"ok","service":"auth-host"}`.
  - `curl http://localhost/api/ai/api/v1/health` → `{"status":"ok","service":"ai-service"}`.
  - `curl http://localhost/api/core/metrics` → Prometheus text with `core_api_up 1` plus billing/posting/intake/risk metrics.

## What is not done
- Runtime validation of migrations (`heads/current/history`) and `alembic_version` table because Docker/DB were unavailable.
- Baseline install test (fresh volume → `alembic upgrade head` → smoke pytest) not executed yet.
- No billing/ledger functional smoke tests executed; Celery queue health not validated.
- “No-danger list” untouched but not enforced yet: Alembic env, naming conventions, baseline tables/enums.

## Risks / next actions
- Resolve the dual Alembic heads by merging `20261201_0017_accounts_and_ledger` into the main line (or rebase onto `0033_billing_periods`), then re-run `alembic heads` to confirm single head.
- Stand up a disposable Postgres (new volume) and run `alembic upgrade head` to confirm baseline applies cleanly; capture `alembic current` and `select * from public.alembic_version;` in this document.
- Run minimum smoke tests (gateway health, `/metrics`, Celery ping via `/api/core/health/celery`, billing smoke if present) and record outputs.
- Keep billing schema frozen until the status/baseline is confirmed to avoid churn in migrations.

## Stage gates A/B/C (status)
- **A — Bootstrap / Dev Stand**: *Partial*. Compose defines all services and health probes; runtime bring-up not verified because Docker is absent. DoD: stack starts (`docker compose up -d`), health/metrics endpoints respond 200, gateway proxies admin/client SPAs.
- **B — Migration Hygiene**: *Blocked*. Two Alembic heads plus prior merge indicate branch divergence. DoD: single head, `alembic current` matches `alembic_version`, history consistent, baseline upgrade on clean DB succeeds.
- **C — Billing MVP**: *Partial/blocked*. Billing tables and metrics exist; no validated billing run or ledger/billing reconciliation smoke. Missing pieces: validated billing_periods lifecycle, invoice generation smoke, payout/settlement flows, and Celery-backed jobs.

## Windows CMD stand-checklist
Run from the repo root after installing Docker Desktop and copying `.env`:

```
bat
docker compose up -d
docker compose ps

curl http://localhost/api/core/health
curl http://localhost/metrics

docker compose run --rm --entrypoint "" core-api sh -lc "alembic -c app/alembic.ini heads"
docker compose run --rm --entrypoint "" core-api sh -lc "alembic -c app/alembic.ini current"
docker compose run --rm --entrypoint "" core-api sh -lc "alembic -c app/alembic.ini history --verbose -r -50:"
docker compose exec -T postgres psql -U neft -d neft -c "select * from public.alembic_version;"
```

Baseline install test (clean volume) should additionally run `docker compose down -v`, `docker compose up -d postgres redis`, `docker compose run --rm core-api alembic -c app/alembic.ini upgrade head`, and a minimal `pytest` smoke suite.
