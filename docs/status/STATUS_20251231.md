# Status & Stage Freeze — 2025-12-31

## Delta since previous status
- Webhooks v1.1: integration-hub-owned partner webhook self-service with replay scheduling, pause/resume, SLA/alerts/metrics + partner portal controls on canonical `/api/int/v1/webhooks/*`; processing-core helpdesk inbound webhooks are a separate contour.
- BI / Analytics / Exports v1.1: BI API, CSV/JSONL exporters + manifest, ClickHouse sync + metrics.
- Portals MAX: client portal and partner portal with i18n + UX polish.
- Marketplace UI flows + event contracts registry (`docs/contracts/events/marketplace`).
- Support / Cases unified contour: canonical `cases` owner, compatibility `support_requests` tail, client helpdesk tickets case-linked, client/partner/admin UI aligned to one lifecycle.
- Client Controls v1: limits/users/services/features tabs + role gating + confirmation modals.
- PWA v1 for client portal: manifest/service worker, PWA routing, push wiring.
- Logistics-service OSRM provider and integration-hub Diadok prod-mode flags.

## Frozen / reference implementations
- Risk v4 reference.
- Unified Explain v1.1.
- Money Flow v3.
- CRM Control Plane freeze guardrails (`X-CRM-Version`).
- Fleet Control v3.

## New capabilities completed
- Integration Hub (EDO + webhook self-service v1.1).
- Document Service (HTML→PDF→S3 + sign/verify).
- Logistics Service (ETA/deviation/explain, mock/OSRM providers).
- BI exports v1.1 (CSV/JSONL + manifest, ClickHouse sync).
- Support / Cases unified contour.
- Marketplace event schemas + portal UI.
- Portals MAX (client/partner/admin).
- Client Controls v1 (UI + API wiring).
- PWA v1 (client portal companion).

## Current runtime status (docker compose)
| Service | Status | Exposed ports | Notes |
| --- | --- | --- | --- |
| core-api | ✅ healthy | 8001→8000 | Health/metrics доступны. |
| gateway | ✅ up | 80 | Проксирует core/auth/ai и SPA. |
| admin-web | ✅ up | 4173→80 | SPA build ok. |
| client-web | ✅ up | 4174→80 | SPA build ok. |
| auth-host | 🟡 up | 8002→8000 | Без Alembic миграций. |
| ai-service | 🟡 up | 8003→8000 | Heuristic scoring provider with explicit source/degraded truth. |
| workers/beat/flower | 🟡 up | 5555 (flower) | Celery tasks + UI. |
| postgres | ✅ up | 5432 | Основная БД. |
| redis | ✅ up | 6379 | Брокер Celery. |
| clickhouse | ✅ up | 8123/9002 | BI mart storage. |
| grafana | 🟡 up | 3000 | Дашборды требуют настройки. |
| prometheus | 🟡 up | 9090 | Scrape /metrics. |
| jaeger | 🟡 up | 16686 | Трассировки частично. |
| otel-collector | 🟡 up | 4317 | Принимает OTLP. |
| minio / minio-init | ✅ up / exited=0 | 9000/9001 | S3-хранилище и init. |
| crm-service | 🟡 up | internal 8000 | Stub health/metrics. |
| logistics-service | ✅ up | internal 8000 | ETA/deviation/explain. |
| document-service | ✅ up | internal 8000 | Render/sign/verify. |
| integration-hub | ✅ up | 8010→8000 | Webhooks + EDO transport owner; schema-aware health. |
| partner-portal | ✅ up | 4175→80 | SPA build ok. |

## CI / quality gates status
- Contracts: `pytest -m contracts`.
- Enum policy: `python scripts/check_enum_policy.py`.
- Smoke: alembic upgrade head (x2) + `pytest -m smoke -q`.
- Migration smoke: protected revisions + revision uniqueness.
- Core API image rebuild: `docker compose build --no-cache core-api`.
- Packaging/installation: editable installs for integration-hub/logistics-service + unit tests.

## Known limitations (facts only)
- auth-host использует bootstrap SQL без Alembic миграций.
- integration-hub по умолчанию использует SQLite (требует внешнюю БД для prod).
- PWA push endpoints `/notifications/*` отсутствуют в backend.
- Client Controls endpoints `/client/users|services|features` are mounted in core-api; users/invitations flow is runtime-verified, while services/features remain read surfaces.
- Diadok prod credentials не включены в репозиторий; default mode is `real`, so missing config degrades explicitly instead of silently switching to mock.
- OSRM runtime не поднимается в compose.
- marketplace backend домен не выделен (есть UI + event contracts).

## Next actions (операционные, без roadmap)
1. Регулярно проверять backlog/replay webhooks и SLA метрики в integration-hub.
2. Контролировать ClickHouse sync lag и BI export SLA.
3. Проверять compose health/metrics и актуальность event registry в `docs/contracts/events/**`.
