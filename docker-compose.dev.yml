services:
  core-api:
    environment:
      APP_ENV: dev
      ALEMBIC_AUTO_REPAIR: 1

  logistics-service:
    environment:
      APP_ENV: dev
      LOGISTICS_PROVIDER: mock
      USE_MOCK_LOGISTICS: 1
