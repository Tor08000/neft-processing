#!/usr/bin/env bash
set -euo pipefail

echo "smoke_client_docflow: placeholder"
# This smoke depends on prepared signed onboarding document and gateway auth context.
# It is intended to be run in CI pipeline with proper env.
